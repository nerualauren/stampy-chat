[tool:pytest]
testpaths = tests
addopts = --cov=src/stampy_chat --cov-report=html --cov-report=term-missing --cov-fail-under=80
markers =
    vcr: tests that use VCR (vcrpy) for recording/replaying API interactions
